@using CapoteSolution.Web.Paginations
@model PaginatedList<CapoteSolution.Web.Models.ViewModels.ServiceDisplayVM>

@{
    ViewData["Title"] = "Servicios Registrados";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>@ViewData["Title"]</h1>
    <a asp-action="CreateService" class="btn btn-success">Nuevo Servicio</a>
</div>

<table class="table table-striped table-hover">
    <thead class="table-dark">
        <tr>
            <th>Fecha</th>           
            <th>Impresora</th>
            <th>Motivo</th>
            <th>Técnico</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.ServiceDate.ToShortDateString()</td>
                <td>@item.CopierInfo</td>
                <td>@item.ServiceReason</td>
                <td>@item.TechnicianName</td>
                <td>
                    <div class="btn-group" role="group">
                        <a asp-action="Details" asp-route-key="@item.Id" class="btn btn-sm btn-info">Detalles</a>
                        <a asp-action="Edit" asp-route-key="@item.Id" class="btn btn-sm btn-primary">Editar</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>